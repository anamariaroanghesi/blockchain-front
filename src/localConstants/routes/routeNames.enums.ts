export enum RouteNamesEnum {
  home = '/',
  festivals = '/festivals',
  festival = '/festival/:id',
  dashboard = '/dashboard',
  unlock = '/unlock',
  disclaimer = '/disclaimer',
  tickets = '/tickets/:id',
  resale = '/resale',
  checkin = '/check-in',
  validate = '/validate'
}
